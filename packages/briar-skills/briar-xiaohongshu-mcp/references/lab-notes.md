# 实验室笔记（2026-09-23）

- Cookie 导出成功后 `login/status` → `is_logged_in: true`，账号示例昵称「爱玩乙游」。
- 首次 publish 若遇 502 且进程消失：多为代理/异常退出；重启后 `curl --noproxy '*'` 可成功。
- 发布成功日志：`发布成功，已跳转离开发布页 url=https://creator.xiaohongshu.com/publish/success?...`
- 用户拒绝 Chrome GUI 自动化时不要再开 computerUse。
