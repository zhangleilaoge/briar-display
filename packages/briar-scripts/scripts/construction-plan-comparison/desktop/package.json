{
	"name": "bid-compare-desktop",
	"version": "1.0.3",
	"description": "施工方案比对工具桌面端",
	"type": "module",
	"private": true,
	"scripts": {
		"dev": "tauri dev",
		"build": "./build.sh",
		"build:mac-intel": "./build.sh mac-intel",
		"build:mac-silicon": "./build.sh mac-silicon",
		"build:windows": "./build.sh windows",
		"release": "./build.sh --release",
		"release:mac-intel": "./build.sh mac-intel --release",
		"release:mac-silicon": "./build.sh mac-silicon --release",
		"release:windows": "./build.sh windows --release",
		"tauri": "tauri"
	},
	"devDependencies": {
		"@tauri-apps/cli": "^2.0.0"
	},
	"dependencies": {
		"@tauri-apps/plugin-dialog": "^2.7.2"
	}
}
